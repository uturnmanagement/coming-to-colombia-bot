"""Colombia Desk SQLite layer — orchestration state only.

The scanner's JSON files in logs/ are preserved untouched. This package
owns deal heartbeat state, snapshot history, and a specialist_reports
placeholder for later layers.
"""
